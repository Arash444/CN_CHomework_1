#ifndef __CMD_HANDLER_H__
#define __CMD_HANDLER_H__

#include <string>
#include <"user.hpp">
#include <vector>

class CmdHandler
{
public:
    CmdHandler();
    std::string runCommand(std::string input);

private:

};

#endif